import ProfileLayout from '@/components/ProfileLayout';
import React from 'react'

const orders = () => {
  return (
    <ProfileLayout>
      <div>order</div>
    </ProfileLayout>
  );
}

export default orders